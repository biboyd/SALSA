name: salsa-env

channels:
  - conda-forge

#NOTE: gcc compiler is needed to install some packages
dependencies:
    - python==3.11
    - astropy
    - h5py
    - matplotlib
    - numpy
    - pip
    - pooch
    - pytest
    - scipy
    - yt==4.2.0
    - pip:
        - mpi4py
        - trident==1.3.0
